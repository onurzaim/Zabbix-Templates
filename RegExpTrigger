# SNMP.Traps : Host Name
# snmp.trap :  Key Name
# str.(VOUTPUT=bad) : string
# VOUTPUT=bad : String(key word) that is crossing inside a trap.
# will send alarm if exsists.

{SNMP.Traps:snmp.trap.str(VOUTPUT=bad)}=1
